Task 49 — the adversarial corpus. No drift files, so no APPLY.sh.

Every file in this archive is either brand new (the corpus/attack tree, the two
new src modules, the three new test files, ADR 0040) or a whole file you have
already shipped from earlier tasks (document.py, loader.py, corpus/__init__.py,
corpus_stats.py, mutate_refusal.py, README.md, SECURITY.md). None is on the
drift list — Makefile, docker-compose.yml, ci.yml, pyproject.toml, uv.lock,
test_runtime.py are all untouched — so this is a plain copy-over-the-top.

The Makefile and CI already do the right thing from tasks 47 and 48:
  - `make corpus` runs scripts/corpus_stats.py, which now prints BOTH the benign
    table and the new adversarial scoreboard.
  - `make prove-refusal` runs scripts/mutate_refusal.py, which now has a sixth
    mutation (blinding the bidi detector), caught by the attack corpus.
  - CI already runs pytest (collects the new tests) and mutate_refusal.py.


APPLY

    cd ~/dev/agent-control-plane
    SRC=/mnt/c/Users/chand/OneDrive/Desktop/acp-task-49
    [ -d "$SRC/acp-task-49" ] && SRC="$SRC/acp-task-49"
    ls "$SRC/src/acp/corpus/attack.py" && cp -r "$SRC/." .

    # the corpus .txt files and everything else arrive mode 755 from Windows;
    # nothing here is executable, so knock them all back to 644:
    find src tests scripts docs corpus -newermt '-20 minutes' -type f -exec chmod 644 {} \;

    make check && make corpus

There is nothing to `git add --chmod=+x` this time — no new scripts. The two
edited scripts (corpus_stats.py, patch_compose_firewall.py is untouched;
corpus_stats.py and mutate_refusal.py) keep whatever mode they already have in
the index.

Then delete this note so it does not land in the repository root.
